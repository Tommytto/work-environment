#!/usr/bin/env node
const chalk = require("chalk");
const clear = require("clear");
const figlet = require("figlet");
const files = require("./lib/files");

// Start
clear();
console.log(
    chalk.blue(figlet.textSync("workio", { horizontalLayout: "full" }))
);

func;
